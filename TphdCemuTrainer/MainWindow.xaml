<Window x:Class="TphdCemuTrainer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TPHD Cemu Trainer"
        Height="690"
        MinHeight="620"
        Width="1040"
        MinWidth="920"
        Background="#F5F7FA"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="14,5" />
            <Setter Property="Margin" Value="4,0" />
        </Style>
        <Style TargetType="ToggleButton">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="MinWidth" Value="60" />
            <Setter Property="Padding" Value="12,5" />
            <Setter Property="Margin" Value="4,0" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Padding" Value="7,4" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Twilight Princess HD Trainer"
                           FontSize="26"
                           FontWeight="SemiBold"
                           Foreground="#152034" />
                <TextBlock Text="External memory editor for Cemu.exe"
                           Margin="0,4,0,0"
                           FontSize="13"
                           Foreground="#657186" />
            </StackPanel>

            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">
                <Button x:Name="AttachButton"
                        Content="Attach / Rescan"
                        Click="AttachButton_Click" />
                <Button x:Name="DetachButton"
                        Content="Detach"
                        IsEnabled="False"
                        Click="DetachButton_Click" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1"
                Margin="0,18,0,16"
                Padding="14"
                CornerRadius="8"
                Background="White"
                BorderBrush="#D9E0EA"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse x:Name="StatusDot"
                             Width="11"
                             Height="11"
                             Fill="#A0A8B5"
                             Margin="0,0,10,0" />
                    <TextBlock x:Name="StatusText"
                               Text="Not attached"
                               FontWeight="SemiBold"
                               Foreground="#24324A" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <TextBlock Text="PID"
                               Foreground="#657186"
                               Margin="0,0,6,0" />
                    <TextBlock x:Name="PidText"
                               Text="-"
                               FontWeight="SemiBold"
                               Margin="0,0,18,0" />
                    <TextBlock Text="Player base"
                               Foreground="#657186"
                               Margin="0,0,6,0" />
                    <TextBlock x:Name="PlayerBaseText"
                               Text="-"
                               FontFamily="Consolas"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2"
                  x:Name="CheatsGrid"
                  ItemsSource="{Binding Cheats}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  RowHeaderWidth="0"
                  AlternatingRowBackground="#F9FBFD"
                  Background="White"
                  BorderBrush="#D9E0EA"
                  BorderThickness="1"
                  IsReadOnly="False"
                  SelectionMode="Single">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Padding" Value="12,10" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="Foreground" Value="#40506A" />
                    <Setter Property="Background" Value="#EEF3F8" />
                    <Setter Property="BorderBrush" Value="#D9E0EA" />
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,6" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Cheat" Width="2.2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"
                                       ToolTip="{Binding Source}"
                                       FontWeight="SemiBold"
                                       Foreground="#152034" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Offset"
                                    Binding="{Binding Offset}"
                                    Width="95"
                                    IsReadOnly="True" />

                <DataGridTextColumn Header="Current"
                                    Binding="{Binding CurrentValue}"
                                    Width="95"
                                    IsReadOnly="True" />

                <DataGridTemplateColumn Header="Target" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBox Text="{Binding DesiredValue, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock Text="{Binding OptionHint}"
                                           Margin="1,3,0,0"
                                           FontSize="10"
                                           Foreground="#778398"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Set" Width="82">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Set"
                                    Click="SetCheat_Click"
                                    Tag="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Max" Width="82">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Max"
                                    Click="MaxCheat_Click"
                                    Tag="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Lock" Width="88">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ToggleButton Content="{Binding LockText}"
                                          IsChecked="{Binding IsLocked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          IsEnabled="{Binding CanLock}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
